apiVersion: v1
kind: ConfigMap
metadata:
  name: auth-config
  labels:
    app: go-webapp
data:
  config.yaml: |
    configuration:
      - user: hello
        path: /foo
      - user: world
        path: /bar